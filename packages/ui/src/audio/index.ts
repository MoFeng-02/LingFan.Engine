/** 08 §六 音频渲染域出口：状态差量规划器 + 渲染器（可测纯逻辑，播放归适配器）。 */
export {
  EMPTY_AUDIO_VIEW,
  createAudioRenderer,
  planAudioActions,
  readAudioView,
  type AudioAction,
  type AudioRenderer,
  type AudioRendererOptions,
  type AudioView,
} from "./audio";
